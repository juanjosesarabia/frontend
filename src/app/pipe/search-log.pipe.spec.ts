import { SearchLogPipe } from './search-log.pipe';

describe('SearchLogPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLogPipe();
    expect(pipe).toBeTruthy();
  });
});
